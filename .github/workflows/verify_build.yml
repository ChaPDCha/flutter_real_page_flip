name: Flutter CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Verify formatting
        run: dart format --set-exit-if-changed .

      - name: Run analyzer
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Analyze example app
        run: |
          cd example
          flutter pub get
          flutter analyze

      - name: Build web (package only)
        run: |
          cd example
          flutter build web --no-pub

      - name: Build Android example
        working-directory: example
        run: flutter build apk --debug --no-pub

      - name: Run Android plugin unit tests
        working-directory: example/android
        run: ./gradlew testDebugUnitTest

  ios:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Prepare local plugin path for Swift Package identity
        run: |
          rm -rf "$GITHUB_WORKSPACE/real_page_flip"
          rsync -a --exclude='example/' --exclude='real_page_flip/' \
            "$GITHUB_WORKSPACE/" "$GITHUB_WORKSPACE/real_page_flip/"
          perl -0pi -e 's#path: \.\./#path: ../real_page_flip#' example/pubspec.yaml

      - name: Install example dependencies
        working-directory: example
        run: flutter pub get

      - name: Build iOS simulator example
        working-directory: example
        run: flutter build ios --simulator --no-codesign --no-pub
