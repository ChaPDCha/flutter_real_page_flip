org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Required by the current Flutter stable Gradle plugin until its AGP 9 new-DSL
# migration is complete. The real_page_flip library itself no longer applies
# the legacy Kotlin Gradle Plugin.
android.builtInKotlin=false
android.newDsl=false
